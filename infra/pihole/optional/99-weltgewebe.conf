# OPTIONAL: activate only together with matching Caddy vhosts + upstream; otherwise causes DNS->gateway without TLS routing.
address=/weltgewebe.home.arpa/<GATEWAY_IP>
# Redundant (covered by wildcard above) but explicit:
# address=/api.weltgewebe.home.arpa/<GATEWAY_IP>
