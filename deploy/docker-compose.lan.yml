services:
  leitstand:
    ports:
      - "${LEITSTAND_BIND_IP:?set LEITSTAND_BIND_IP}:3000:3000"
      # Optional: Example for explicit bind IP (uncomment and adjust if needed):
      # - "192.168.178.10:3000:3000"
